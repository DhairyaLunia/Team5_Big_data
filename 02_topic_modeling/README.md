# 02 Topic Modeling

This stage uses Gensim LDA to extract 25 topics from a 300K review sample. It applies NLTK stopwords and lemmatization.

It outputs the `lda_topic_results` table containing 4.76M rows, with an average of 13.9 topics per review.
