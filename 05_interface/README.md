# 05 Interface

This stage implements the Streamlit web application. It features two user flows (existing users vs. new users), displays a persona card, provides chat input for the recommender agent, and shows result cards containing vibe tags and review citations. 

It includes 5 pre-configured demo queries for evaluation.
