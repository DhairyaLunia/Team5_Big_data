# 01 Data Processing

This stage loads the Yelp JSON dataset, filters the data for Philadelphia, applies a ≥50 review threshold, and joins the reviews, businesses, and users tables.

It outputs `merged_philly` and `businesses_filtered` temp views.
